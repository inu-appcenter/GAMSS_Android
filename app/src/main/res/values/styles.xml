<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="progressBarHorizontal" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:background">@drawable/progress_vote</item>
        <item name="android:max">100</item>
        <item name="android:progress">82</item>
        <item name="android:progressTint">@color/color_3effde</item>
        <item name="android:progressDrawable">@drawable/progress_vote</item>
    </style>

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">#00000000</item>
    </style>

</resources>